requests
# ========================================
# Realtime HALfred - Core Dependencies
# ========================================

# Environment configuration
python-dotenv>=1.0.0

# Audio I/O (requires platform-specific setup - see README)
# - macOS: Works out of the box (Core Audio)
# - Windows: PortAudio DLL automatically installed with pip
# - Linux: Install libportaudio2 first (apt-get install libportaudio2)
sounddevice>=0.4.6

# Text-to-Speech via ElevenLabs API
elevenlabs>=1.0.0

# Keyboard input for push-to-talk functionality
# - Works on macOS, Windows, and Linux
# - macOS: May require Accessibility permission in System Preferences
pynput>=1.7.6

# OpenAI Agents SDK (Realtime API + MCP support)
openai-agents>=0.6.0

# OpenAI Python SDK (Responses API used by Supervisor/Anki subagent)
openai>=1.0.0

# MCP (Model Context Protocol) server support
mcp>=1.0.0

# ========================================
# Desktop Automation Dependencies (Computer-Control-MCP)
# ========================================

# UV - Required to run computer-control-mcp via uvx
# Computer-Control-MCP is installed and run via: uvx computer-control-mcp@latest
# This installs PyAutoGUI, RapidOCR, ONNXRuntime automatically
uv>=0.1.0

# PyAutoGUI - Optional direct fallback for desktop automation
# Only needed if ENABLE_COMPUTER_CONTROL_MCP=false and you want direct PyAutoGUI access
# Install with: pip install pyautogui
# pyautogui>=0.9.54

# Pillow - Optional for screenshot support on Windows/Linux
# macOS uses native screencapture command (no dependencies)
# Windows/Linux require Pillow for screenshot functionality
# Install with: pip install Pillow
# Pillow>=10.0.0

# ========================================
# Submodule Dependencies
# ========================================

# ScreenMonitorMCP - Install separately after cloning submodule:
#   git submodule update --init --recursive
#   cd ScreenMonitorMCP
#   pip install -e .
#
# This provides AI vision and screen monitoring capabilities (✅ STABLE)
